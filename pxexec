#!/usr/bin/env perl 

use strict;
use warnings;
use Data::Uniqid qw/suniqid/;

my $cmd = join ' ', @ARGV;
my $host = `uname -n`;
chomp $host;
my $prop = 'PXEXEC_' . $host . '_' . suniqid;
system('xprop', '-f', $prop, '8s', '-root', '-set', $prop, $cmd);
